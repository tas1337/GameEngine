[package]
name = "game_engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

# ONLY wasm-bindgen for JavaScript interop - everything else built from scratch
[dependencies]
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
js-sys = "0.3"
console_error_panic_hook = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1.3"
base64 = "0.21"
image = { version = "0.24", default-features = false, features = ["jpeg", "png"] }

[dependencies.web-sys]
version = "0.3"
features = [
    # DOM basics
    "Window",
    "Document",
    "HtmlCanvasElement",
    "Element",
    "HtmlElement",
    "HtmlImageElement",
    "Node",
    "console",
    "CssStyleDeclaration",
    "Location",
    
    # WebGL - Raw 3D graphics (like Three.js uses)
    "WebGlRenderingContext",
    "WebGlProgram",
    "WebGlShader",
    "WebGlBuffer",
    "WebGlUniformLocation",
    "WebGlTexture",
    "WebGlFramebuffer",
    "WebGlRenderbuffer",
    "WebGl2RenderingContext",
    
    # Input
    "KeyboardEvent",
    "MouseEvent",
    "WheelEvent",
    "EventTarget",
    
    # Performance
    "Performance",
    
    # WebSocket for multiplayer
    "WebSocket",
    "BinaryType",
    "MessageEvent",
    "ErrorEvent",
    "CloseEvent",
    
    # Fetch API for loading GLTF
    "Request",
    "RequestInit",
    "RequestMode",
    "Response",
    "Headers",
    "Blob",
    "ImageBitmap",
]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

